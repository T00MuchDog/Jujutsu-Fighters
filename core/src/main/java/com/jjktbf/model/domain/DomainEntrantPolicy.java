package com.jjktbf.model.domain;

/** How combatants materializing after establishment join a Domain space. */
public enum DomainEntrantPolicy {
    SNAPSHOT,
    FOLLOW_SUMMONER,
    ALL_NEW_COMBATANTS
}
